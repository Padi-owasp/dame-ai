package ai.dame.business.game;

/** Kind of piece: an ordinary man (Stein) or a promoted flying king (Dame). */
public enum PieceType {
    /** Ordinary man (Stein). */
    MAN,
    /** Promoted flying king (Dame). */
    DAME
}
